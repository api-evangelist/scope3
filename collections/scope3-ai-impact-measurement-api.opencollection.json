{
  "opencollection": "1.0.0",
  "info": {
    "name": "AI Impact Measurement API",
    "version": "1.0.0"
  },
  "request": {
    "auth": {
      "type": "bearer",
      "token": "{{bearerToken}}"
    }
  },
  "items": [
    {
      "info": {
        "name": "AI Impact Measurement",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Calculate AI model impact metrics for BigQuery",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "{{baseUrl}}/",
            "body": {
              "type": "json",
              "data": "{}"
            },
            "auth": {
              "type": "bearer",
              "token": "{{bearerToken}}"
            }
          },
          "docs": "Processes batched impact calculations for BigQuery remote function calls"
        }
      ]
    }
  ],
  "bundled": true
}